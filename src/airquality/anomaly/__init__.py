"""Anomaly-detection benchmark over air-quality series, with two modes.

A stripped, renamed fork of the genias benchmark library: detector models and
the benchmark plots, wired into an end-to-end benchmark (load → preprocess →
detect → ensemble → results.json) with two evaluation modes:

- ``unlabeled`` (default, matches production): no ground truth — scores are
  binarized per segment with a median + k·MAD threshold and detectors whose
  per-station detection rate exceeds ``max_detection_rate`` (default 7%) are
  discarded locally; each station's survivors form its consensus ensemble.
- ``synthetic``: anomalies (:data:`~airquality.anomaly.benchmark.INJECTION_VARIANT`,
  ``combined`` shapes) are injected directly into the real series — no STL
  synthetic base — and detectors are scored with the supervised metric set
  (VUS-PR et al.); the ensemble mirrors ``inject-vote`` with local rankings on
  long segments and the station-level selection ranking on short segments.

Plots are generated by the separate
:mod:`airquality.visualizations.anomaly_benchmark` script. The ``GenIAS`` VAE
keeps its name (it backs the ``*GenIAS`` detector variants, which synthesize
anomalous *training* windows internally).
"""

from __future__ import annotations

from .benchmark import (
    INJECTION_VARIANT,
    MODES,
    AnomalyBenchmarkConfig,
    AnomalyCase,
    build_cases,
    run_benchmark,
)
from .ensemble import rank_top_k
from .metrics import (
    DEFAULT_MAX_DETECTION_RATE,
    DEFAULT_THRESHOLD_K,
    compute_metrics,
    compute_segmented_metrics,
    detect_mask,
    mad_threshold,
    vus_sliding_window,
    vus_sliding_window_segments,
)
from .registry import MODEL_REGISTRY, resolve_model_names

__all__ = [
    "AnomalyCase",
    "AnomalyBenchmarkConfig",
    "DEFAULT_MAX_DETECTION_RATE",
    "DEFAULT_THRESHOLD_K",
    "INJECTION_VARIANT",
    "MODEL_REGISTRY",
    "MODES",
    "build_cases",
    "compute_metrics",
    "compute_segmented_metrics",
    "detect_mask",
    "mad_threshold",
    "rank_top_k",
    "resolve_model_names",
    "run_benchmark",
    "vus_sliding_window",
    "vus_sliding_window_segments",
]
